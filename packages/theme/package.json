{
  "name": "@casino/theme",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'theme build'",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for pure design tokens'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
