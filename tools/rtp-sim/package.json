{
  "name": "@casino/rtp-sim",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'rtp-sim build'",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "sim": "tsx src/cli.ts"
  },
  "dependencies": {
    "@casino/rng": "workspace:*",
    "@casino/slot-runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
